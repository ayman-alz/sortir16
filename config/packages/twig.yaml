twig:
  default_path: '%kernel.project_dir%/templates'
  form_theme: 'bootstrap_5_layout.html.twig'
  globals:
    app_image_profil_directory: 'uploads/images/profil'

when@test:
  twig:
    strict_variables: true
